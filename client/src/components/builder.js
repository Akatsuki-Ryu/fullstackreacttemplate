import React, {Component} from 'react';

class Builder extends Component {
    render() {
        return (
            <div>
                the builder
            </div>
        );
    }
}

export default Builder;
