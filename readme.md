this is akabox .
i am working on the fullstack template .


this a react + redux + express + mongodb + nginx + docker full stack template

to run it , install docker first .

build from source code


`npm install`

`docker-compose up`

it will open 3000 port for client debug and 80 for client prod, 3001 for backend enterface . database is in a private network
